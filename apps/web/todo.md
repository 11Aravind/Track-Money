updation
no separate menu wanted for category.
if user want to add category then show the popup and in the top setion 3 tabs expense, inome ,transfer. in the eah tab show default expense also incomes with icon + name user can select anything . if  also availeble to add new category. with + icon and add btn. if click this  then user can enter the category name and  user can selet the color, and icon(here lot of icon listed).
if user can add expence or income then select the category and dd thng .so first select attegory and do it.
